let's add an option to save/export the current site with database and everything includings apis and images and bookings, and let's add a way to import

generating an api code should give the user all the neccesary info like agency name, api endpoint, api access key 
